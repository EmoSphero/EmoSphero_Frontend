import React, { Component } from "react";

export default class Button extends Component {
  render() {
    return (
      <div>
        <button onClick={() => console.log("Hi")}>wer</button>
        <button onClick={}>wer</button>

      </div>
    );
  }
}
